diff --git a/public/index.html b/public/index.html
index d3c4998..580c6b3 100644
--- a/public/index.html
+++ b/public/index.html
@@ -5100,58 +5100,108 @@
       </div>
 
       <!-- 繧ｵ繝ｼ繝舌・險ｭ螳壹Δ繝ｼ繝繝ｫ -->
-      <div id="serverSettingsModal" class="fixed inset-0 modal-overlay overflow-y-auto z-50 hidden">
-        <div class="flex min-h-full items-center justify-center p-6">
-          <div class="bg-white rounded-2xl shadow-2xl w-full max-w-lg relative overflow-hidden modal-box animate-pop-in">
-            <button id="closeServerSettingsBtn"
-              class="absolute top-3.5 right-4 text-gray-400 hover:text-gray-700 z-10"><i
-                class="fas fa-times text-lg"></i></button>
-            <div class="bg-gray-800 text-white px-6 py-5 flex-shrink-0 modal-dark-header flex items-center justify-between">
-              <div>
-                <p class="text-white/70 text-xs font-bold uppercase tracking-wider">繧ｵ繝ｼ繝舌・險ｭ螳・/p>
-                <p id="serverSettingsTitle" class="font-bold text-xl mt-0.5"></p>
+      <!-- 繧ｵ繝ｼ繝舌・險ｭ螳壹Δ繝ｼ繝繝ｫ (PC: Discord鬚ｨ2繧ｫ繝ｩ繝) -->
+      <div id="serverSettingsModal" class="fixed inset-0 modal-overlay flex items-center justify-center z-50 hidden p-4">
+        <div class="bg-white dark:bg-[#313338] rounded-2xl shadow-2xl w-full max-w-4xl overflow-hidden modal-box animate-pop-in" style="height:min(700px,calc(100dvh - 3rem))">
+          <div class="settings-discord-layout flex h-full">
+            <!-- 蟾ｦ繧ｵ繧､繝峨ヰ繝ｼ -->
+            <div class="settings-sidebar w-[240px] flex-shrink-0 flex flex-col overflow-y-auto" style="background-color: #2b2d31; padding: 60px 10px 20px 20px;">
+              <div class="settings-sidebar-user" style="border-bottom: none; padding: 0 10px 10px;">
+                <span class="settings-sidebar-name font-black text-white" id="serverSettingsTitle" style="font-size: 1.05rem; white-space: pre-wrap; word-break: break-word;">繧ｵ繝ｼ繝舌・蜷・/span>
               </div>
-              <!-- 繧ｵ繝ｼ繝舌・繧｢繧､繧ｳ繝ｳ險ｭ螳啅I -->
-              <div id="serverIconSettingsWrapper" class="relative group cursor-pointer hidden">
-                <div id="serverIconSettingsPreview" class="w-20 h-20 rounded-full bg-slate-800 flex items-center justify-center text-white font-bold text-3xl shadow-lg border-2 border-white/20 transition-all duration-300 group-hover:rounded-3xl overflow-hidden"></div>
-                <div class="absolute inset-0 bg-slate-900/60 rounded-full flex flex-col items-center justify-center opacity-0 group-hover:opacity-100 group-hover:rounded-3xl transition-all duration-300">
-                  <i class="fas fa-camera text-white text-xl mb-0.5"></i>
-                  <span class="text-[9px] text-white font-bold tracking-wider">螟画峩縺吶ｋ</span>
+              <div class="settings-nav-divider mx-2 my-2 bg-white/10 h-px"></div>
+              
+              <button class="settings-nav-item active flex items-center gap-2 px-3 py-1.5 rounded text-gray-300 hover:bg-white/5 hover:text-white transition-colors" id="ssTabOverview" onclick="switchServerSettingsTab('overview')">
+                讎りｦ・+              </button>
+              <button class="settings-nav-item flex items-center gap-2 px-3 py-1.5 rounded text-gray-300 hover:bg-white/5 hover:text-white transition-colors" id="ssTabRooms" onclick="switchServerSettingsTab('rooms')">
+                繝√Ε繝ｳ繝阪Ν繝ｻ繧ｫ繝・ざ繝ｪ繝ｼ
+              </button>
+              <button class="settings-nav-item flex items-center gap-2 px-3 py-1.5 rounded text-gray-300 hover:bg-white/5 hover:text-white transition-colors" id="ssTabMembers" onclick="switchServerSettingsTab('members')">
+                繝｡繝ｳ繝舌・
+              </button>
+              <button class="settings-nav-item flex items-center gap-2 px-3 py-1.5 rounded text-gray-300 hover:bg-white/5 hover:text-white transition-colors" id="ssTabInvites" onclick="switchServerSettingsTab('invites')">
+                諡帛ｾ・+              </button>
+              <button class="settings-nav-item flex items-center gap-2 px-3 py-1.5 rounded text-gray-300 hover:bg-white/5 hover:text-white transition-colors" id="ssTabStamps" onclick="switchServerSettingsTab('stamps')">
+                繧ｹ繧ｿ繝ｳ繝・+              </button>
+              
+              <div class="settings-nav-divider mx-2 my-3 bg-white/10 h-px"></div>
+              
+              <button class="settings-nav-item danger flex items-center gap-2 px-3 py-1.5 rounded text-red-400 hover:bg-red-500 hover:text-white transition-colors" onclick="switchServerSettingsTab('danger')">
+                繧ｵ繝ｼ繝舌・繧貞炎髯､
+              </button>
+            </div>
+            
+            <!-- 蜿ｳ繧ｳ繝ｳ繝・Φ繝・-->
+            <div class="settings-content relative flex-1 overflow-y-auto" style="background-color: #313338; color: white; padding: 60px 40px 40px;">
+              <button id="closeServerSettingsBtn" class="absolute top-10 right-10 text-gray-400 hover:text-gray-200 transition-colors flex flex-col items-center justify-center border-2 border-transparent hover:border-gray-600 rounded-full w-9 h-9" title="髢峨§繧・(ESC)"><i class="fas fa-times text-lg"></i><span style="font-size:9px;font-weight:bold;margin-top:2px;">ESC</span></button>
+              
+              <!-- 讎りｦ√ち繝・-->
+              <div id="ssOverviewSection" class="settings-content-section active space-y-8 max-w-xl">
+                <h2 class="text-xl font-bold text-white mb-6">繧ｵ繝ｼ繝舌・縺ｮ讎りｦ・/h2>
+                
+                <div class="flex flex-col md:flex-row gap-8">
+                  <div class="flex-shrink-0 flex flex-col items-center gap-3">
+                    <div id="serverIconSettingsWrapper" class="relative group cursor-pointer">
+                      <div id="serverIconSettingsPreview" class="w-[100px] h-[100px] rounded-full bg-gray-700 flex items-center justify-center text-white font-bold text-3xl shadow-lg border-2 border-transparent transition-all duration-300 group-hover:rounded-[30%] overflow-hidden"></div>
+                      <div class="absolute inset-0 bg-black/60 rounded-full flex flex-col items-center justify-center opacity-0 group-hover:opacity-100 group-hover:rounded-[30%] transition-all duration-300">
+                        <i class="fas fa-camera text-white text-xl mb-1"></i>
+                        <span class="text-[10px] text-white font-bold tracking-wider">繧｢繧､繧ｳ繝ｳ螟画峩</span>
+                      </div>
+                      <input type="file" id="serverIconUploadInput" accept="image/*" class="hidden" />
+                    </div>
+                    <div class="text-[10px] text-gray-400 text-center leading-tight">譛蟆上し繧､繧ｺ: 128x128</div>
+                  </div>
+                  
+                  <div class="flex-1 space-y-4">
+                    <div>
+                      <label class="block text-gray-300 text-xs font-bold uppercase tracking-wider mb-2">繧ｵ繝ｼ繝舌・蜷・/label>
+                      <input type="text" id="renameServerInput" placeholder="譁ｰ縺励＞繧ｵ繝ｼ繝舌・蜷・ class="w-full p-2.5 bg-[#1e1f22] border border-transparent rounded text-white focus:outline-none focus:border-indigo-500 transition-colors" />
+                    </div>
+                  </div>
+                </div>
+                
+                <div class="border-t border-gray-700/50 pt-6 mt-6 flex justify-end gap-3">
+                    <p id="serverSettingsMessage" class="text-sm font-medium flex-1 self-center"></p>
+                    <button id="renameServerBtn" class="bg-indigo-500 hover:bg-indigo-600 text-white px-5 py-2.5 rounded text-sm font-bold transition-colors shadow-md">螟画峩繧剃ｿ晏ｭ・/button>
                 </div>
-                <input type="file" id="serverIconUploadInput" accept="image/*" class="hidden" />
               </div>
-            </div>
-            <!-- 繧ｿ繝・-->
-            <div class="flex gap-2 px-5 py-3 bg-white border-b border-gray-100 overflow-x-auto whitespace-nowrap hide-scrollbar">
-              <button id="ssTabRooms" class="ss-tab active">繝ｫ繝ｼ繝</button>
-              <button id="ssTabMembers" class="ss-tab">繝｡繝ｳ繝舌・</button>
-              <button id="ssTabInvites" class="ss-tab">諡帛ｾ・/button>
-              <button id="ssTabStamps" class="ss-tab">繧ｹ繧ｿ繝ｳ繝・/button>
-              <button id="ssTabDanger" class="ss-tab">縺昴・莉・/button>
-            </div>
-            <div class="p-6 max-h-[60vh] overflow-y-auto">
-              <!-- 繝ｫ繝ｼ繝繧ｿ繝・-->
-              <div id="ssRoomsSection">
-                <div class="flex gap-2 mb-4">
-                  <input type="text" id="newRoomNameInput" placeholder="譁ｰ縺励＞繝ｫ繝ｼ繝蜷・
-                    class="flex-1 p-2 border border-gray-300 rounded-lg focus:outline-none text-sm" />
-                  <button id="createRoomInServerBtn"
-                    class="bg-gray-800 hover:bg-gray-700 text-white px-4 py-2 rounded-lg text-sm font-bold transition-all">霑ｽ蜉</button>
+              
+              <!-- 繝ｫ繝ｼ繝繝ｻ繧ｫ繝・ざ繝ｪ繝ｼ邂｡逅・ち繝・-->
+              <div id="ssRoomsSection" class="settings-content-section hidden space-y-6 max-w-2xl">
+                <h2 class="text-xl font-bold text-white mb-6">繝√Ε繝ｳ繝阪Ν縺ｨ繧ｫ繝・ざ繝ｪ繝ｼ</h2>
+                <div class="flex flex-col md:flex-row gap-3 mb-4">
+                  <div class="flex-1 flex gap-2">
+                    <input type="text" id="newRoomNameInput" placeholder="譁ｰ縺励＞繝ｫ繝ｼ繝蜷・ class="flex-1 p-2.5 bg-[#1e1f22] rounded focus:outline-none text-sm text-white" />
+                    <select id="newRoomCategorySelect" class="p-2.5 bg-[#1e1f22] rounded focus:outline-none text-sm text-white border-l border-gray-800">
+                      <option value="">繧ｫ繝・ざ繝ｪ繝ｼ縺ｪ縺・/option>
+                    </select>
+                    <button id="createRoomInServerBtn" class="bg-indigo-500 hover:bg-indigo-600 text-white px-5 py-2.5 rounded text-sm font-bold transition-colors">霑ｽ蜉</button>
+                  </div>
+                  <button id="createCategoryBtn" class="bg-[#4e5058] hover:bg-[#6d6f78] text-white px-4 py-2.5 rounded text-sm font-bold whitespace-nowrap transition-colors"><i class="fas fa-folder-plus mr-1"></i>繧ｫ繝・ざ繝ｪ繝ｼ菴懈・</button>
                 </div>
-                <div id="serverRoomsList" class="space-y-2"></div>
+                <div class="h-px bg-gray-700 w-full my-4"></div>
+                <div id="serverRoomsList" class="space-y-4 max-h-[60vh] overflow-y-auto pr-2 custom-scrollbar"></div>
               </div>
+
               <!-- 繝｡繝ｳ繝舌・繧ｿ繝・-->
-              <div id="ssMembersSection" class="hidden space-y-2">
-                <div id="serverMembersManageList"></div>
+              <div id="ssMembersSection" class="settings-content-section hidden space-y-6 max-w-2xl">
+                <h2 class="text-xl font-bold text-white mb-6">繝｡繝ｳ繝舌・邂｡逅・/h2>
+                <div class="bg-[#2b2d31] rounded-lg p-4">
+                   <div id="serverMembersManageList" class="space-y-2"></div>
+                </div>
               </div>
+              
               <!-- 諡帛ｾ・ち繝・-->
-              <div id="ssInvitesSection" class="hidden">
-                <div class="mb-4 space-y-2">
-                  <div class="grid grid-cols-2 gap-2">
+              <div id="ssInvitesSection" class="settings-content-section hidden space-y-6 max-w-2xl">
+                <h2 class="text-xl font-bold text-white mb-6">諡帛ｾ・Μ繝ｳ繧ｯ</h2>
+                <div class="bg-[#2b2d31] p-5 rounded-lg space-y-4">
+                  <div class="grid grid-cols-2 gap-4">
                     <div>
-                      <label class="text-xs text-gray-500 block mb-1">譛牙柑譛滄剞</label>
-                      <select id="inviteExpiry"
-                        class="w-full p-2 border border-gray-300 rounded-lg text-sm focus:outline-none">
+                      <label class="text-xs font-bold text-gray-400 block mb-2 uppercase tracking-wider">譛牙柑譛滄剞</label>
+                      <select id="inviteExpiry" class="w-full p-2.5 bg-[#1e1f22] text-white rounded focus:outline-none focus:ring-1 focus:ring-indigo-500">
                         <option value="0">縺ｪ縺・/option>
                         <option value="1">1譌･</option>
                         <option value="7">7譌･</option>
@@ -5159,9 +5209,8 @@
                       </select>
                     </div>
                     <div>
-                      <label class="text-xs text-gray-500 block mb-1">菴ｿ逕ｨ蝗樊焚</label>
-                      <select id="inviteMaxUses"
-                        class="w-full p-2 border border-gray-300 rounded-lg text-sm focus:outline-none">
+                      <label class="text-xs font-bold text-gray-400 block mb-2 uppercase tracking-wider">菴ｿ逕ｨ蝗樊焚</label>
+                      <select id="inviteMaxUses" class="w-full p-2.5 bg-[#1e1f22] text-white rounded focus:outline-none focus:ring-1 focus:ring-indigo-500">
                         <option value="0">辟｡蛻ｶ髯・/option>
                         <option value="1">1蝗・/option>
                         <option value="5">5蝗・/option>
@@ -5169,51 +5218,44 @@
                       </select>
                     </div>
                   </div>
-                  <button id="createInviteCodeBtn"
-                    class="w-full bg-gray-800 hover:bg-gray-700 text-white font-bold py-2.5 rounded-xl text-sm transition-all">諡帛ｾ・さ繝ｼ繝峨ｒ逕滓・</button>
+                  <button id="createInviteCodeBtn" class="w-full mt-4 bg-indigo-500 hover:bg-indigo-600 text-white font-bold py-3 rounded text-sm transition-colors shadow-md">譁ｰ縺励＞繝ｪ繝ｳ繧ｯ繧堤函謌・/button>
                 </div>
+                <div class="h-px bg-gray-700 w-full my-6"></div>
+                <h3 class="text-xs font-bold text-gray-400 uppercase tracking-wider mb-3">譛牙柑縺ｪ諡帛ｾ・Μ繝ｳ繧ｯ</h3>
                 <div id="inviteCodesList" class="space-y-2"></div>
               </div>
+              
               <!-- 繧ｹ繧ｿ繝ｳ繝励ち繝・-->
-              <div id="ssStampsSection" class="hidden space-y-4">
-                <div class="bg-gray-50 border border-gray-200 p-6 rounded-2xl flex flex-col items-center justify-center text-center">
-                  <div class="w-16 h-16 bg-white rounded-full flex items-center justify-center shadow-sm mb-4">
-                    <i class="fas fa-icons text-3xl text-gray-500"></i>
+              <div id="ssStampsSection" class="settings-content-section hidden space-y-6 max-w-2xl">
+                <h2 class="text-xl font-bold text-white mb-6">繧ｹ繧ｿ繝ｳ繝・/h2>
+                <div class="bg-[#2b2d31] p-8 rounded-lg flex flex-col items-center justify-center text-center">
+                  <div class="w-20 h-20 bg-gray-700 rounded-full flex items-center justify-center shadow-md mb-5">
+                    <i class="fas fa-laugh-squint text-4xl text-gray-400"></i>
                   </div>
-                  <h3 class="text-lg font-bold text-gray-800 mb-6">繧ｹ繧ｿ繝ｳ繝礼ｮ｡逅・/h3>
-                  <button onclick="openStampAdminModal()" class="bg-gray-800 hover:bg-gray-900 text-white font-bold py-3 px-8 rounded-xl shadow-md transition-all">
+                  <h3 class="text-lg font-bold text-white mb-2">繧ｵ繝ｼ繝舌・逕ｨ繧ｹ繧ｿ繝ｳ繝・/h3>
+                  <p class="text-gray-400 text-sm mb-6 max-w-sm">縺薙・繧ｵ繝ｼ繝舌・蟆ら畑縺ｮ繧ｫ繧ｹ繧ｿ繝繧ｹ繧ｿ繝ｳ繝励ｒ霑ｽ蜉繝ｻ邂｡逅・〒縺阪∪縺吶・/p>
+                  <button onclick="openStampAdminModal()" class="bg-indigo-500 hover:bg-indigo-600 text-white font-bold py-2.5 px-8 rounded transition-colors shadow-md">
                     繧ｹ繧ｿ繝ｳ繝礼ｮ｡逅・ｒ髢九￥
                   </button>
                 </div>
               </div>
-              <!-- 縺昴・莉悶ち繝・-->
-              <div id="ssDangerSection" class="hidden space-y-4">
-                <div>
-                  <label class="block text-xs font-bold text-gray-500 uppercase tracking-wider mb-1">繧ｵ繝ｼ繝舌・蜷阪ｒ螟画峩</label>
-                  <div class="flex gap-2">
-                    <input type="text" id="renameServerInput" placeholder="譁ｰ縺励＞繧ｵ繝ｼ繝舌・蜷・
-                      class="flex-1 p-2 border border-gray-300 rounded-lg text-sm focus:outline-none" />
-                    <button id="renameServerBtn"
-                      class="bg-gray-600 hover:bg-gray-700 text-white px-3 py-2 rounded-lg text-sm font-bold">螟画峩</button>
-                  </div>
-                </div>
-                <div>
-                  <label class="block text-xs font-bold text-gray-500 uppercase tracking-wider mb-1">繝代せ繝ｯ繝ｼ繝峨ｒ螟画峩</label>
-                  <div class="space-y-2">
-                    <input type="password" id="changeServerPasswordInput" placeholder="譁ｰ縺励＞繝代せ繝ｯ繝ｼ繝・
-                      class="w-full p-2 border border-gray-300 rounded-lg text-sm focus:outline-none" />
-                    <button id="changeServerPasswordBtn"
-                      class="w-full bg-gray-600 hover:bg-gray-700 text-white font-bold py-2 rounded-lg text-sm">繝代せ繝ｯ繝ｼ繝牙､画峩</button>
+              
+              <!-- 蜑企勁繧ｿ繝・-->
+              <div id="ssDangerSection" class="settings-content-section hidden space-y-6 max-w-xl">
+                <h2 class="text-xl font-bold text-red-500 mb-6">繧ｵ繝ｼ繝舌・縺ｮ蜑企勁</h2>
+                <div class="bg-[#2b2d31] p-5 rounded-lg border border-red-500/20">
+                  <p class="text-gray-300 text-sm mb-5 leading-relaxed">譛ｬ蠖薙↓縺薙・繧ｵ繝ｼ繝舌・繧貞炎髯､縺励∪縺吶°・・br>縺薙・謫堺ｽ懊ｒ陦後≧縺ｨ縲√☆縺ｹ縺ｦ縺ｮ繝｡繝・そ繝ｼ繧ｸ縲√メ繝｣繝ｳ繝阪Ν縲√ョ繝ｼ繧ｿ縺悟ｮ悟・縺ｫ螟ｱ繧上ｌ縲∝叙繧頑ｶ医☆縺薙→縺ｯ縺ｧ縺阪∪縺帙ｓ縲・/p>
+                  <div class="space-y-4">
+                    <div>
+                      <label class="block text-xs font-bold text-gray-400 mb-2 uppercase tracking-wider">邂｡逅・畑繝代せ繝ｯ繝ｼ繝峨ｒ蜈･蜉・/label>
+                      <input type="password" id="changeServerPasswordInput" placeholder="繝代せ繝ｯ繝ｼ繝峨ｒ蜈･蜉・.." class="w-full p-2.5 bg-[#1e1f22] border border-transparent rounded text-white focus:outline-none focus:border-red-500 transition-colors" />
+                    </div>
+                    <button id="deleteServerBtn" class="bg-red-600 hover:bg-red-700 text-white font-bold py-2.5 px-6 rounded text-sm transition-colors shadow-md mt-2">繧ｵ繝ｼ繝舌・繧貞炎髯､縺吶ｋ</button>
                   </div>
                 </div>
-                <div class="pt-2 border-t border-red-100">
-                  <button id="deleteServerBtn"
-                    class="w-full bg-red-500 hover:bg-red-600 text-white font-bold py-2.5 rounded-xl text-sm transition-all"><i
-                      class="fas fa-trash mr-1"></i>縺薙・繧ｵ繝ｼ繝舌・繧貞炎髯､</button>
-                </div>
               </div>
+              
             </div>
-            <p id="serverSettingsMessage" class="text-center text-sm px-6 pb-2 font-medium"></p>
           </div>
         </div>
       </div>
@@ -5973,134 +6015,110 @@
       return _e2ee._roomKeyPromises[roomId];
     }
     async function _getOrCreateRoomKeyImpl(serverId, roomId, memberIds) {
-      const ok = await ensureE2EEKeys();
-      if (!ok) {
-        console.warn(`[E2EE] 繝ｦ繝ｼ繧ｶ繝ｼ骰ｵ縺梧悴蛻晄悄蛹悶・縺溘ａ繝ｫ繝ｼ繝骰ｵ繧貞叙蠕励〒縺阪∪縺帙ｓ (room=${roomId})`);
-        return null;
-      }
+      if (!_subtleOK) return null;
 
-      // 1) 閾ｪ蛻・ｮ帙・wrapped room key縺熊irestore/D1縺ｫ縺ゅｋ縺九ｒ縺ｾ縺壹メ繧ｧ繝・け
       try {
-        let data = null;
-        
-          const myWrapRef = doc(db, `artifacts/${appId}/servers/${serverId}/rooms/${roomId}/roomKeys/${userId}`);
-          const myWrapSnap = await getDoc(myWrapRef);
-          if (myWrapSnap.exists()) data = myWrapSnap.data();
-        
+        // 1) 蜈ｱ譛峨Ν繝ｼ繝繧ｭ繝ｼ (Shared Room Key) 縺後☆縺ｧ縺ｫ繝ｫ繝ｼ繝繝峨く繝･繝｡繝ｳ繝医↓蟄伜惠縺吶ｋ縺九メ繧ｧ繝・け
+        const rSnap = await getDoc(doc(db, `artifacts/${appId}/servers/${serverId}/rooms/${roomId}`));
+        let roomData = null;
+        if (rSnap.exists()) {
+          roomData = rSnap.data();
+          if (roomData.sharedKey) {
+             try {
+                // 蜈ｱ譛峨く繝ｼ縺悟ｭ伜惠縺吶ｋ蝣ｴ蜷医・繧､繝ｳ繝昴・繝医＠縺ｦ蛻ｩ逕ｨ
+                const raw = _b64ToAb(roomData.sharedKey);
+                const key = await window.crypto.subtle.importKey("raw", raw, { name: "AES-GCM" }, true, ["encrypt", "decrypt"]);
+                const keysObj = { "1": key, latest: key, latestVersion: "1" };
+                _e2ee.roomKeyCache[roomId] = keysObj;
+                return keysObj;
+             } catch(e) {
+                console.warn("[SharedKey] 蜈ｱ譛峨く繝ｼ縺ｮ繧､繝ｳ繝昴・繝医↓螟ｱ謨・", e);
+             }
+          }
+        }
 
-        let keysObj = {};
-        if (data) {
-          const versions = data.versions || {};
-          for (const k in data) {
-            if (k.startsWith("versions.")) {
-              const ver = k.replace("versions.", "");
-              versions[ver] = data[k];
+        // 2) 蜈ｱ譛峨Ν繝ｼ繝繧ｭ繝ｼ縺悟ｭ伜惠縺励↑縺・ｴ蜷医・℃蜴ｻ縺ｮE2EE・医Ξ繧ｬ繧ｷ繝ｼ・蛾嵯繧呈爾縺・+        const ok = await ensureE2EEKeys();
+        if (ok) {
+          let legacyKey = null;
+          const myWrapSnap = await getDoc(doc(db, `artifacts/${appId}/servers/${serverId}/rooms/${roomId}/roomKeys/${userId}`));
+          if (myWrapSnap.exists()) {
+            const data = myWrapSnap.data();
+            let wrappedStr = data.wrappedKey;
+            if (data.versions && data.latestVersion && data.versions[data.latestVersion]) {
+              wrappedStr = data.versions[data.latestVersion];
+            } else if (data[`versions.${data.latestVersion}`]) {
+              wrappedStr = data[`versions.${data.latestVersion}`];
+            }
+            if (wrappedStr) {
+               try {
+                 const raw = await window.crypto.subtle.decrypt({ name: "RSA-OAEP" }, _e2ee.privateKey, _b64ToAb(wrappedStr));
+                 legacyKey = await window.crypto.subtle.importKey("raw", raw, { name: "AES-GCM" }, true, ["encrypt", "decrypt"]);
+               } catch(e) {}
             }
           }
-          if (data.wrappedKey && !versions["1"]) versions["1"] = data.wrappedKey; // migration
-          data.versions = versions;
           
-          let maxVer = 1;
-          for (const ver in versions) {
-             try {
-               const raw = await window.crypto.subtle.decrypt({ name: "RSA-OAEP" }, _e2ee.privateKey, _b64ToAb(versions[ver]));
-               const key = await window.crypto.subtle.importKey("raw", raw, { name: "AES-GCM" }, true, ["encrypt", "decrypt"]);
-               keysObj[ver] = key;
-               if (parseInt(ver) > maxVer) maxVer = parseInt(ver);
-             } catch(e) {}
-          }
-          if (Object.keys(keysObj).length > 0) {
-             const latestVerStr = data.latestVersion ? data.latestVersion.toString() : maxVer.toString();
-             keysObj.latest = keysObj[latestVerStr] || keysObj[Object.keys(keysObj).pop()];
-             keysObj.latestVersion = latestVerStr;
-             _e2ee.roomKeyCache[roomId] = keysObj;
-             return keysObj;
+          // 邂｡逅・・お繧ｹ繧ｯ繝ｭ繝ｼ縺九ｉ縺ｮ蠕ｩ譌ｧ
+          if (!legacyKey && isAdmin) {
+             const escWrapSnap = await getDoc(doc(db, `artifacts/${appId}/servers/${serverId}/rooms/${roomId}/roomKeys/__escrow__`));
+             if (escWrapSnap.exists()) {
+               try {
+                 const escPrivSnap = await getDoc(doc(db, `artifacts/${appId}/users/${userId}/private/escrowKey`));
+                 if (escPrivSnap.exists() && escPrivSnap.data().privateKeyJwk) {
+                   const escrowPrivKey = await _importPriv(escPrivSnap.data().privateKeyJwk);
+                   const data = escWrapSnap.data();
+                   let wrappedStr = data.wrappedKey;
+                   if (data.versions && data.latestVersion && data.versions[data.latestVersion]) wrappedStr = data.versions[data.latestVersion];
+                   else if (data[`versions.${data.latestVersion}`]) wrappedStr = data[`versions.${data.latestVersion}`];
+                   if (wrappedStr) {
+                     const raw = await window.crypto.subtle.decrypt({ name: "RSA-OAEP" }, escrowPrivKey, _b64ToAb(wrappedStr));
+                     legacyKey = await window.crypto.subtle.importKey("raw", raw, { name: "AES-GCM" }, true, ["encrypt", "decrypt"]);
+                   }
+                 }
+               } catch(e) {}
+             }
           }
-        }
 
-        // 邂｡逅・・お繧ｹ繧ｯ繝ｭ繝ｼ骰ｵ繝輔か繝ｼ繝ｫ繝舌ャ繧ｯ・域悽莠ｺ縺ｮ繝ｫ繝ｼ繝骰ｵ縺悟ｭ伜惠縺励↑縺・√∪縺溘・蠕ｩ蜿ｷ縺ｧ縺阪↑縺・ｴ蜷茨ｼ・-        if (isAdmin && Object.keys(keysObj).length === 0) {
-          let escrowKeyData = null;
-          
-            const escWrapSnap = await getDoc(doc(db, `artifacts/${appId}/servers/${serverId}/rooms/${roomId}/roomKeys/__escrow__`));
-            if (escWrapSnap.exists()) escrowKeyData = escWrapSnap.data();
-          
-          if (escrowKeyData) {
-            let escrowPrivJwk = null;
-            
-              try {
-                const escPrivSnap = await getDoc(doc(db, `artifacts/${appId}/users/${userId}/private/escrowKey`));
-                if (escPrivSnap.exists() && escPrivSnap.data().privateKeyJwk) escrowPrivJwk = escPrivSnap.data().privateKeyJwk;
-              } catch(e){}
-            
-            if (escrowPrivJwk) {
-              try {
-                const escrowPrivKey = await _importPriv(escrowPrivJwk);
-                const versions = escrowKeyData.versions || {};
-                for (const k in escrowKeyData) {
-                  if (k.startsWith("versions.")) {
-                    const ver = k.replace("versions.", "");
-                    versions[ver] = escrowKeyData[k];
-                  }
-                }
-                if (escrowKeyData.wrappedKey && !versions["1"]) versions["1"] = escrowKeyData.wrappedKey;
-                
-                let maxVer = 1;
-                const escKeysObj = {};
-                for (const ver in versions) {
-                   try {
-                     const raw = await window.crypto.subtle.decrypt({ name: "RSA-OAEP" }, escrowPrivKey, _b64ToAb(versions[ver]));
-                     const key = await window.crypto.subtle.importKey("raw", raw, { name: "AES-GCM" }, true, ["encrypt", "decrypt"]);
-                     escKeysObj[ver] = key;
-                     if (parseInt(ver) > maxVer) maxVer = parseInt(ver);
-                   } catch(e) {}
-                }
-                if (Object.keys(escKeysObj).length > 0) {
-                   const latestVerStr = escrowKeyData.latestVersion ? escrowKeyData.latestVersion.toString() : maxVer.toString();
-                   escKeysObj.latest = escKeysObj[latestVerStr] || escKeysObj[Object.keys(escKeysObj).pop()];
-                   escKeysObj.latestVersion = latestVerStr;
-                   _e2ee.roomKeyCache[roomId] = escKeysObj;
-                   return escKeysObj;
-                }
-              } catch(e) { console.warn("[E2EE] 繧ｨ繧ｹ繧ｯ繝ｭ繝ｼ骰ｵ縺ｧ縺ｮ蠕ｩ蜿ｷ縺ｫ螟ｱ謨・", e); }
-            }
+          // 繝ｬ繧ｬ繧ｷ繝ｼ繧ｭ繝ｼ縺悟ｾｩ蜈・〒縺阪◆蝣ｴ蜷医√◎繧後ｒ蜈ｱ譛峨く繝ｼ縺ｨ縺励※菫晏ｭ倥＠縺ｦ繧｢繝・・繧ｰ繝ｬ繝ｼ繝・+          if (legacyKey) {
+             console.log(`[SharedKey] 繝ｬ繧ｬ繧ｷ繝ｼE2EE繧ｭ繝ｼ繧貞ｾｩ蜈・＠縺ｾ縺励◆縲ょ・譛峨く繝ｼ縺ｨ縺励※繧｢繝・・繧ｰ繝ｬ繝ｼ繝峨＠縺ｾ縺・(room=${roomId})`);
+             try {
+                const raw = await window.crypto.subtle.exportKey("raw", legacyKey);
+                const b64 = _abToB64(raw);
+                await updateDoc(doc(db, `artifacts/${appId}/servers/${serverId}/rooms/${roomId}`), { sharedKey: b64, currentKeyVersion: 1 });
+                const keysObj = { "1": legacyKey, latest: legacyKey, latestVersion: "1" };
+                _e2ee.roomKeyCache[roomId] = keysObj;
+                return keysObj;
+             } catch(e) {
+                console.warn("[SharedKey] 蜈ｱ譛峨く繝ｼ縺ｸ縺ｮ繧｢繝・・繧ｰ繝ｬ繝ｼ繝峨↓螟ｱ謨・", e);
+             }
           }
         }
-      } catch (e) {
-        console.error(`[E2EE] 閾ｪ蛻・ｮ帙Ν繝ｼ繝骰ｵ縺ｮ蠕ｩ蜿ｷ縺ｫ螟ｱ謨暦ｼ亥ｹｳ譁・〒邯咏ｶ壹る嵯縺悟・逕滓・縺輔ｌ縺溷庄閭ｽ諤ｧ・・(room=${roomId}):`, e);
-      }
 
-      // 縲先怙驥崎ｦ√ヶ繝ｭ繝・け蜃ｦ逅・代Ν繝ｼ繝縺ｫ譌｢縺ｫ繝｡繝・そ繝ｼ繧ｸ縺悟ｭ伜惠縺吶ｋ・茨ｼ晞℃蜴ｻ縺ｫ骰ｵ縺御ｽ懊ｉ繧後※縺・ｋ縺ｯ縺夲ｼ峨・縺ｫ閾ｪ蛻・・謇句・縺ｫ骰ｵ縺檎┌縺・ｴ蜷医・-      // 蜍晄焔縺ｫ譁ｰ縺励＞繝ｫ繝ｼ繝骰ｵ繧堤函謌舌＠縺ｦ莉悶・繝ｦ繝ｼ繧ｶ繝ｼ縺ｮ骰ｵ繧剃ｸ頑嶌縺咲ｴ螢奇ｼ医せ繝励Μ繝・ヨ繝悶Ξ繧､繝ｳ・峨☆繧九・繧堤ｵｶ蟇ｾ縺ｫ髦ｲ縺舌・-      try {
+        // 3) 縺昴ｌ縺ｧ繧ゅく繝ｼ縺後↑縺・ｴ蜷医√ｂ縺鈴℃蜴ｻ繝｡繝・そ繝ｼ繧ｸ縺悟ｭ伜惠縺吶ｋ縺ｪ繧我ｸ頑嶌縺阪ｒ驕ｿ縺代ｋ縺溘ａ繝悶Ο繝・け
         let hasExistingData = false;
+        if (roomData && roomData.currentKeyVersion > 1) hasExistingData = true;
+        const msgsSnap = await getDocs(query(collection(db, `artifacts/${appId}/servers/${serverId}/rooms/${roomId}/messages`), limit(1)));
+        if (!msgsSnap.empty) hasExistingData = true;
         
-          const msgsSnap = await getDocs(query(collection(db, `artifacts/${appId}/servers/${serverId}/rooms/${roomId}/messages`), limit(1)));
-          if (!msgsSnap.empty) hasExistingData = true;
-          const rSnap = await getDoc(doc(db, `artifacts/${appId}/servers/${serverId}/rooms/${roomId}`));
-          if (rSnap.exists() && rSnap.data().currentKeyVersion > 1) hasExistingData = true;
-        
-
         if (hasExistingData) {
-          console.warn(`[E2EE] 繝ｫ繝ｼ繝(room=${roomId})縺ｫ縺ｯ譌｢縺ｫ驕主悉繝ｭ繧ｰ縺悟ｭ伜惠縺励∪縺吶よ焔蜈・↓骰ｵ縺後↑縺・◆繧√∵眠隕冗函謌舌↓繧医ｋ荳頑嶌縺咲ｴ螢翫ｒ繝悶Ο繝・け縺励∪縺励◆縲らｮ｡逅・・↓繧医ｋ蜷磯嵯(繧ｨ繧ｹ繧ｯ繝ｭ繝ｼ)縺九ｉ縺ｮ閾ｪ蜍墓舞貂医ｒ蠕・ｩ溘＠縺ｾ縺吶Ａ);
+          console.warn(`[SharedKey] 繝ｫ繝ｼ繝(room=${roomId})縺ｫ縺ｯ譌｢縺ｫ驕主悉繝ｭ繧ｰ縺悟ｭ伜惠縺励∪縺吶よ焔蜈・↓骰ｵ縺後↑縺・◆繧√∵眠隕冗函謌舌↓繧医ｋ荳頑嶌縺咲ｴ螢翫ｒ繝悶Ο繝・け縺励∪縺励◆縲Ａ);
           await requestEscrowRescue(serverId, roomId);
           return null;
         }
-      } catch (e) {
-        console.warn("[E2EE] 驕主悉繝｡繝・そ繝ｼ繧ｸ遒ｺ隱堺ｸｭ縺ｫ繧ｨ繝ｩ繝ｼ縺檎匱逕溘＠縺ｾ縺励◆縲ゆｸ頑嶌縺阪ｒ髦ｲ豁｢縺吶ｋ縺溘ａ譁ｰ隕城嵯逕滓・繧剃ｸｭ譁ｭ縺励∪縺・", e);
-        return null;
-      }
 
-      // 2) 辟｡縺代ｌ縺ｰ譁ｰ隕冗函謌舌＠縺ｦ驟榊ｸ・-      try {
+        // 4) 螳悟・縺ｫ譁ｰ隕上・蝣ｴ蜷医∵眠縺励＞蜈ｱ譛峨Ν繝ｼ繝繧ｭ繝ｼ繧堤函謌舌＠縺ｦ繝峨く繝･繝｡繝ｳ繝医↓菫晏ｭ・         const key = await window.crypto.subtle.generateKey({ name: "AES-GCM", length: 256 }, true, ["encrypt", "decrypt"]);
         const raw = await window.crypto.subtle.exportKey("raw", key);
-        await _distributeRoomKeyVersion(serverId, roomId, raw, memberIds, 1);
+        const b64 = _abToB64(raw);
+        await updateDoc(doc(db, `artifacts/${appId}/servers/${serverId}/rooms/${roomId}`), { sharedKey: b64, currentKeyVersion: 1 });
+        
         const keysObj = { "1": key, latest: key, latestVersion: "1" };
         _e2ee.roomKeyCache[roomId] = keysObj;
         return keysObj;
+
       } catch (e) {
-        console.warn("[E2EE] 繝ｫ繝ｼ繝骰ｵ縺ｮ逕滓・/驟榊ｸ・↓螟ｱ謨暦ｼ亥ｹｳ譁・〒邯咏ｶ夲ｼ・", e);
+        console.error(`[SharedKey] 繝ｫ繝ｼ繝骰ｵ縺ｮ蜿門ｾ励・逕滓・蜃ｦ逅・↓螟ｱ謨・(room=${roomId}):`, e);
         return null;
       }
     }
@@ -6655,6 +6673,7 @@
       bundlePolicy: 'max-bundle',
     };
     let messageLimit = 20;
+    let rtdbMessagesLimit = 20;
     let allowPagination = false;
     // 繝壹・繧ｸ繝阪・繧ｷ繝ｧ繝ｳ逕ｨ: 蜈ｨ繝ｭ繝ｼ繝画ｸ医∩繝｡繝・そ繝ｼ繧ｸ・・nSnapshot縺ｮ譛譁ｰ20莉ｶ + 霑ｽ蜉隱ｭ縺ｿ霎ｼ縺ｿ蛻・ｼ・     let allLoadedMessages = []; // { ...msgData, id } 縺ｮ驟榊・・亥商縺・・ｼ・@@ -8512,6 +8531,8 @@
       avatarUploadInput.value = '';
     });
 
+
+
     // Settings Modal Logic
     const resetAvatarButton = document.getElementById("resetAvatarButton");
 
@@ -9864,7 +9885,11 @@
         snapshot.forEach(docSnap => roomDocs.push(docSnap));
         roomDocs.sort((a, b) => (a.data().order || 0) - (b.data().order || 0));
 
-        roomDocs.forEach((docSnap, idx) => {
+        let categories = currentServerData?.categories || [];
+        if(!Array.isArray(categories)) categories = Object.values(categories);
+        categories.sort((a,b) => a.order - b.order);
+
+        const renderSingleRoom = (docSnap) => {
           currentRoomIds.add(docSnap.id);
           const room = docSnap.data();
           roomNames[docSnap.id] = room.name;
@@ -9907,9 +9932,45 @@
 
           div.appendChild(nameDiv);
           div.appendChild(rightContainer);
-          roomList.appendChild(div);
+          return div;
+        };
+
+        // 1. Categories and their rooms
+        categories.forEach(cat => {
+           const catRooms = roomDocs.filter(d => d.data().categoryId === cat.id);
+           
+           const catDiv = document.createElement("div");
+           catDiv.className = "flex items-center px-1 mt-4 mb-1 text-[11px] font-bold text-slate-400 dark:text-slate-500 uppercase tracking-wider select-none group";
+           catDiv.innerHTML = `<i class="fas fa-chevron-down mr-1.5 text-[9px] transition-transform"></i>${escapeHtml(cat.name)}`;
+           roomList.appendChild(catDiv);
+           
+           if(catRooms.length > 0) {
+              const roomContainer = document.createElement("div");
+              catRooms.forEach(d => {
+                  roomContainer.appendChild(renderSingleRoom(d));
+              });
+              roomList.appendChild(roomContainer);
+           } else {
+              // Empty category placeholder? (Optional: could hide or show something else)
+           }
         });
 
+        // 2. Uncategorized rooms
+        const uncatRooms = roomDocs.filter(d => !d.data().categoryId);
+        if(uncatRooms.length > 0) {
+           if(categories.length > 0) {
+               const uncatDiv = document.createElement("div");
+               uncatDiv.className = "flex items-center px-1 mt-4 mb-1 text-[11px] font-bold text-slate-400 dark:text-slate-500 uppercase tracking-wider select-none";
+               uncatDiv.innerHTML = `<i class="fas fa-minus mr-1.5 text-[9px]"></i>縺昴・莉冒;
+               roomList.appendChild(uncatDiv);
+           }
+           const roomContainer = document.createElement("div");
+           uncatRooms.forEach(d => {
+               roomContainer.appendChild(renderSingleRoom(d));
+           });
+           roomList.appendChild(roomContainer);
+        }
+
         // covo_global_items 縺ｯ updateGlobalNotifUI 繧・requestScanAllUnread 縺ｧ邂｡逅・＆繧後ｋ縺溘ａ縲・         // 縺薙％縺ｧ縺ｮ boolean 菫晏ｭ・covo_server_unread)縺ｯ蟒・ｭ｢縺励∪縺励◆縲・       }
@@ -10053,7 +10114,21 @@
                 await decryptMessagesInPlace(docs, currentServerId, currentRoomId, _members).catch(() => {});
                 
                 if (docs.length > 0) {
+                   // RTDB's older docs are prepended if we maintain chronological order.
+                   // Actually, allLoadedMessages is currently descending (newest first).
+                   // docs is also descending. older docs have smaller timestamps, so they should go to the end of the array.
                    allLoadedMessages = [...allLoadedMessages, ...docs];
+                   
+                   // De-duplicate just in case
+                   const seen = new Set();
+                   allLoadedMessages = allLoadedMessages.filter(m => {
+                       if(seen.has(m.id)) return false;
+                       seen.add(m.id);
+                       return true;
+                   });
+                   // Sort descending just to be safe
+                   allLoadedMessages.sort((a, b) => b.timestamp - a.timestamp);
+                   
                    lastMessagesData = [...allLoadedMessages];
                    renderMessagesWithReadReceipts();
                    rtdbMessagesLimit += docs.length; // keep limit expanded
@@ -10095,19 +10170,25 @@
               await decryptMessagesInPlace(olderDocs, currentServerId, currentRoomId, _members).catch(() => {});
               
               allLoadedMessages = [...allLoadedMessages, ...olderDocs];
+              
+              // De-duplicate just in case
+              const seen = new Set();
+              allLoadedMessages = allLoadedMessages.filter(m => {
+                  if(seen.has(m.id)) return false;
+                  seen.add(m.id);
+                  return true;
+              });
+              // Sort descending just to be safe
+              allLoadedMessages.sort((a, b) => b.timestamp - a.timestamp);
+
               lastMessagesData = [...allLoadedMessages];
               renderMessagesWithReadReceipts();
               
+              // Rather than a stub, we should completely restart the onSnapshot with the new limit
+              // so that new messages still arrive perfectly while old messages are retained.
               if (unsubscribeMessages) { unsubscribeMessages(); unsubscribeMessages = null; }
-      if (window.rtdbMessagesUnsub) { window.rtdbMessagesUnsub(); window.rtdbMessagesUnsub = null; }
-              const newQ = query(
-                collection(db, `artifacts/${appId}/servers/${currentServerId}/rooms/${currentRoomId}/messages`),
-                orderBy("timestamp", "desc"),
-                limit(messageLimit)
-              );
-              unsubscribeMessages = onSnapshot(newQ, async (snap) => {
-                  // Reduced for brevity in script, rely on main subscriber update
-              });
+              if (window.rtdbMessagesUnsub) { window.rtdbMessagesUnsub(); window.rtdbMessagesUnsub = null; }
+              subscribeToMessagesFirestore(false); // Restart with new limit, false = not initial
             }
         }
       } catch (e) {
@@ -10230,7 +10311,10 @@
       rtdbMessagesUnsub = () => { off(q); };
     }
 
-    function subscribeToMessagesFirestore() {
+    function subscribeToMessagesFirestore(isInitial = true) {
+      if(isInitial) {
+         isInitialMessageLoad = true;
+      }
       const q = query(
         collection(db, `artifacts/${appId}/servers/${currentServerId}/rooms/${currentRoomId}/messages`),
         orderBy("timestamp", "desc"),
@@ -10278,13 +10362,17 @@
             allLoadedMessages = [...realTimeMessagesCache];
           }
         } else {
-          const latestIds = new Set(latestMessages.map(m => m.id));
-          const olderKeptCache = realTimeMessagesCache.filter(m => !latestIds.has(m.id));
-          realTimeMessagesCache = [...olderKeptCache, ...latestMessages];
+          // latestMessages contains the full sliding window of messages according to `messageLimit`
+          // We should just use it as our cache.
+          realTimeMessagesCache = [...latestMessages];
 
           if (!isJumpView) {
+            // Merge gracefully. Keep older loaded messages that exceed the limit.
+            const latestIds = new Set(latestMessages.map(m => m.id));
             const olderKeptMessages = allLoadedMessages.filter(m => !latestIds.has(m.id));
-            allLoadedMessages = [...olderKeptMessages, ...latestMessages];
+            allLoadedMessages = [...latestMessages, ...olderKeptMessages];
+            // Sort descending just to be safe
+            allLoadedMessages.sort((a, b) => b.timestamp - a.timestamp);
           } else {
             const latestMap = new Map(latestMessages.map(m => [m.id, m]));
             jumpViewMessages = jumpViewMessages.map(m => latestMap.has(m.id) ? latestMap.get(m.id) : m);
@@ -10953,17 +11041,25 @@
     document.getElementById("closeServerSettingsBtn").addEventListener("click", () => serverSettingsModal.classList.add("hidden"));
 
     // 繧ｵ繝ｼ繝舌・險ｭ螳壹・繧ｿ繝・-    let currentSsTab = "rooms";
-    function switchSsTab(tab) {
+    let currentSsTab = "overview";
+    window.switchServerSettingsTab = function(tab) {
       currentSsTab = tab;
-      ["rooms", "members", "invites", "stamps", "danger"].forEach(t => {
-        document.getElementById(`ssTab${t.charAt(0).toUpperCase() + t.slice(1)}`).classList.toggle("active", t === tab);
-        document.getElementById(`ss${t.charAt(0).toUpperCase() + t.slice(1)}Section`).classList.toggle("hidden", t !== tab);
+      ["overview", "rooms", "members", "invites", "stamps", "danger"].forEach(t => {
+        const tabEl = document.getElementById(`ssTab${t.charAt(0).toUpperCase() + t.slice(1)}`);
+        const secEl = document.getElementById(`ss${t.charAt(0).toUpperCase() + t.slice(1)}Section`);
+        if(tabEl) tabEl.classList.toggle("active", t === tab);
+        if(secEl) {
+            secEl.classList.toggle("hidden", t !== tab);
+            secEl.classList.toggle("active", t === tab);
+        }
       });
-    }
-    ["Rooms", "Members", "Invites", "Stamps", "Danger"].forEach(t => {
-      document.getElementById(`ssTab${t}`).addEventListener("click", () => switchSsTab(t.toLowerCase()));
-    });
+      
+      // 繧ｿ繝悶＃縺ｨ縺ｮ隱ｭ縺ｿ霎ｼ縺ｿ
+      if (tab === "rooms") loadServerSettingsRooms();
+      if (tab === "members") loadServerSettingsMembers();
+      if (tab === "invites") loadInviteCodes();
+      if (tab === "stamps") loadServerStampsForAdmin();
+    };
 
     async function openServerSettings() {
       if (!currentServerId) return;
@@ -10972,14 +11068,14 @@
 
       const isOwner = currentServerData?.createdBy === userId ||
         (currentServerData?.serverAdmins && currentServerData.serverAdmins.includes(userId));
-      const hasAdminRights = isOwner || isAdmin;
+      const hasAdminRights = isOwner || isAdmin || isListAdmin;
 
       // 繧ｵ繝ｼ繝舌・繧｢繧､繧ｳ繝ｳ險ｭ螳壹・陦ｨ遉ｺ繝ｻ譖ｴ譁ｰ
       const iconWrapper = document.getElementById("serverIconSettingsWrapper");
       const iconPreview = document.getElementById("serverIconSettingsPreview");
       if (iconWrapper && iconPreview) {
-        iconWrapper.style.display = (hasAdminRights || isListAdmin) ? "" : "none";
-        iconWrapper.classList.toggle("hidden", !(hasAdminRights || isListAdmin));
+        iconWrapper.style.display = hasAdminRights ? "" : "none";
+        iconWrapper.classList.toggle("hidden", !hasAdminRights);
         if (currentServerData?.iconUrl) {
           iconPreview.innerHTML = `<img src="${currentServerData.iconUrl}" class="w-full h-full object-cover" />`;
           iconPreview.style.backgroundColor = "transparent";
@@ -10990,111 +11086,114 @@
         }
       }
 
-      // 蜑企勁繝懊ち繝ｳ縺ｯ邂｡逅・・髄縺・-      const deleteBtn = document.getElementById("deleteServerBtn");
-      if (deleteBtn) {
-        deleteBtn.style.display = hasAdminRights ? "" : "none";
-      }
-
-      // 繧ｿ繝悶・陦ｨ遉ｺ蛻ｶ蠕｡
-      const tabs = ["ssTabRooms", "ssTabMembers", "ssTabInvites", "ssTabDanger"];
-      tabs.forEach(tabId => {
+      // 繧ｿ繝悶・陦ｨ遉ｺ蛻ｶ蠕｡ (讓ｩ髯舌↓繧医ｋ蜃ｺ縺怜・縺・
+      const adminTabs = ["ssTabOverview", "ssTabRooms", "ssTabMembers", "ssTabInvites", "ssDangerSection"];
+      adminTabs.forEach(tabId => {
         const el = document.getElementById(tabId);
         if (el) el.style.display = hasAdminRights ? "" : "none";
       });
+      
+      const dangerTab = document.querySelector('.settings-nav-item.danger');
+      if (dangerTab) dangerTab.style.display = hasAdminRights ? "" : "none";
 
       openModal(serverSettingsModal);
       
       if (hasAdminRights) {
-        switchSsTab("rooms");
-        await loadServerSettingsRooms();
+        switchServerSettingsTab("overview");
       } else {
-        switchSsTab("stamps");
-        await loadServerStampsForAdmin();
+        switchServerSettingsTab("stamps");
       }
     }
 
     async function loadServerSettingsRooms() {
       const listEl = document.getElementById("serverRoomsList");
+      const categorySelect = document.getElementById("newRoomCategorySelect");
       listEl.innerHTML = "<p class='text-xs text-gray-400'>隱ｭ縺ｿ霎ｼ縺ｿ荳ｭ...</p>";
       let roomsData = [];
       try {
-        
-          const snap = await getDocs(collection(db, `artifacts/${appId}/servers/${currentServerId}/rooms`));
-          let idx = 0;
-          snap.forEach(d => { roomsData.push({ id: d.id, ...d.data(), order: typeof d.data().order === 'number' ? d.data().order : (idx++) * 10 }); });
-        
+        const snap = await getDocs(collection(db, `artifacts/${appId}/servers/${currentServerId}/rooms`));
+        let idx = 0;
+        snap.forEach(d => { roomsData.push({ id: d.id, ...d.data(), order: typeof d.data().order === 'number' ? d.data().order : (idx++) * 10 }); });
       } catch (e) { console.error(e); }
 
-      roomsData.sort((a, b) => a.order - b.order);
+      // Get categories
+      const serverSnap = await getDoc(doc(db, `artifacts/${appId}/servers/${currentServerId}`));
+      const serverData = serverSnap.data();
+      let categories = serverData.categories || [];
+      if(!Array.isArray(categories)) categories = Object.values(categories);
       
-      // DB荳翫・order蛟､縺碁㍾隍・∪縺溘・譛ｪ險ｭ螳壹・迥ｶ諷九ｒ髦ｲ縺舌◆繧√∵紛豬√＆繧後◆order蛟､繧堤｢ｺ菫・+      categories.sort((a,b) => a.order - b.order);
+      
+      // Update select
+      categorySelect.innerHTML = `<option value="">繧ｫ繝・ざ繝ｪ繝ｼ縺ｪ縺・/option>`;
+      categories.forEach(cat => {
+        const opt = document.createElement("option");
+        opt.value = cat.id;
+        opt.textContent = cat.name;
+        categorySelect.appendChild(opt);
+      });
+
+      roomsData.sort((a, b) => a.order - b.order);
       roomsData.forEach((r, i) => { r.order = i * 10; });
 
-      // 繝｡繝｢繝ｪ荳翫・譛譁ｰ繝ｪ繧ｹ繝医ｒ菴ｿ縺｣縺ｦDOM繧呈ｧ狗ｯ峨・蜊ｳ譎よ峩譁ｰ縺吶ｋ髢｢謨ｰ
       function renderRoomsListUI() {
         listEl.innerHTML = "";
-        roomsData.sort((a, b) => a.order - b.order);
         
-        roomsData.forEach((room, index) => {
+        const renderRoomItem = (room, index, arr) => {
           const item = document.createElement("div");
-          item.className = "flex items-center justify-between p-3 bg-gray-50 border border-gray-200 rounded-lg group transition-all";
+          item.className = "flex items-center justify-between p-3 bg-[#2b2d31] border border-[#1e1f22] rounded-lg group transition-all ml-4 mb-2";
           const nameSpan = document.createElement("span");
-          nameSpan.className = "text-sm font-medium text-gray-900";
-          nameSpan.textContent = `# ${room.name}`;
+          nameSpan.className = "text-sm font-medium text-gray-300 flex items-center gap-2";
+          nameSpan.innerHTML = `<i class="fas fa-hashtag text-gray-500 text-xs"></i> ${escapeHtml(room.name)}`;
 
           const btnsContainer = document.createElement("div");
           btnsContainer.className = "flex items-center gap-1.5";
 
-          // 荳ｦ縺ｳ譖ｿ縺井ｸ翫∈繝懊ち繝ｳ
+          // Order Up
           const upBtn = document.createElement("button");
-          upBtn.className = `room-order-btn p-1.5 rounded-lg text-xs font-bold transition-all ${index === 0 ? 'text-gray-300 cursor-not-allowed' : 'text-gray-600 hover:text-black hover:bg-gray-200'}`;
+          upBtn.className = `room-order-btn p-1.5 rounded-lg text-xs font-bold transition-all ${index === 0 ? 'text-gray-600 cursor-not-allowed' : 'text-gray-400 hover:text-white hover:bg-white/10'}`;
           upBtn.disabled = index === 0;
           upBtn.innerHTML = `<i class="fas fa-arrow-up"></i>`;
           upBtn.addEventListener("click", async () => {
             if (index === 0) return;
-            // 騾｣謇薙ｄ遶ｶ蜷医ｒ髦ｲ縺舌◆繧∝・繝懊ち繝ｳ繧貞叉蠎ｧ縺ｫ繝ｭ繝・け縺励√Ο繝ｼ繝・ぅ繝ｳ繧ｰ迥ｶ諷九ｒ陦ｨ遉ｺ
             listEl.querySelectorAll(".room-order-btn, .del-room-btn").forEach(b => { b.disabled = true; b.style.opacity = "0.5"; });
             upBtn.innerHTML = `<i class="fas fa-spinner fa-spin"></i>`;
             
-            const prev = roomsData[index - 1];
+            const prev = arr[index - 1];
             const tempOrder = room.order;
             room.order = prev.order;
             prev.order = tempOrder;
 
             await updateRoomOrderBoth(room.id, room.order);
             await updateRoomOrderBoth(prev.id, prev.order);
-            
-            // 繝｡繝｢繝ｪ荳翫・繧ｽ繝ｼ繝育憾諷九ｒ逶ｴ謗･蜿肴丐縺励※鬮倬溘°縺､遒ｺ螳溘↑繝ｬ繝ｳ繝繝ｪ繝ｳ繧ｰ
             renderRoomsListUI();
             if (typeof loadServerRooms === 'function') loadServerRooms(currentServerId);
           });
 
-          // 荳ｦ縺ｳ譖ｿ縺井ｸ九∈繝懊ち繝ｳ
+          // Order Down
           const downBtn = document.createElement("button");
-          downBtn.className = `room-order-btn p-1.5 rounded-lg text-xs font-bold transition-all ${index === roomsData.length - 1 ? 'text-gray-300 cursor-not-allowed' : 'text-gray-600 hover:text-black hover:bg-gray-200'}`;
-          downBtn.disabled = index === roomsData.length - 1;
+          downBtn.className = `room-order-btn p-1.5 rounded-lg text-xs font-bold transition-all ${index === arr.length - 1 ? 'text-gray-600 cursor-not-allowed' : 'text-gray-400 hover:text-white hover:bg-white/10'}`;
+          downBtn.disabled = index === arr.length - 1;
           downBtn.innerHTML = `<i class="fas fa-arrow-down"></i>`;
           downBtn.addEventListener("click", async () => {
-            if (index === roomsData.length - 1) return;
+            if (index === arr.length - 1) return;
             listEl.querySelectorAll(".room-order-btn, .del-room-btn").forEach(b => { b.disabled = true; b.style.opacity = "0.5"; });
             downBtn.innerHTML = `<i class="fas fa-spinner fa-spin"></i>`;
 
-            const next = roomsData[index + 1];
+            const next = arr[index + 1];
             const tempOrder = room.order;
             room.order = next.order;
             next.order = tempOrder;
 
             await updateRoomOrderBoth(room.id, room.order);
             await updateRoomOrderBoth(next.id, next.order);
-
             renderRoomsListUI();
             if (typeof loadServerRooms === 'function') loadServerRooms(currentServerId);
           });
 
-          // 蜑企勁繝懊ち繝ｳ
+          // Delete
           const delBtn = document.createElement("button");
-          delBtn.className = "del-room-btn text-red-500 hover:text-red-700 hover:bg-red-100 p-1.5 rounded-lg text-xs transition-all ml-2";
+          delBtn.className = "del-room-btn text-red-500 hover:text-red-400 hover:bg-red-500/10 p-1.5 rounded-lg text-xs transition-all ml-2";
           delBtn.dataset.roomId = room.id;
           delBtn.dataset.roomName = room.name;
           delBtn.innerHTML = `<i class="fas fa-trash"></i>`;
@@ -11122,8 +11221,68 @@
           btnsContainer.appendChild(delBtn);
           item.appendChild(nameSpan);
           item.appendChild(btnsContainer);
-          listEl.appendChild(item);
+          return item;
+        };
+
+        // Render categories and rooms inside them
+        categories.forEach((cat) => {
+          const catDiv = document.createElement("div");
+          catDiv.className = "mb-4";
+          
+          const catHeader = document.createElement("div");
+          catHeader.className = "flex items-center justify-between mb-2 p-2 bg-[#1e1f22] rounded-lg";
+          catHeader.innerHTML = `
+            <span class="text-xs font-bold text-gray-400 uppercase tracking-wider flex items-center gap-1.5">
+              <i class="fas fa-folder-open text-[10px]"></i> ${escapeHtml(cat.name)}
+            </span>
+            <div class="flex gap-1">
+              <button class="cat-del-btn text-gray-500 hover:text-red-400 p-1" data-cat-id="${cat.id}"><i class="fas fa-trash text-sm"></i></button>
+            </div>
+          `;
+          catDiv.appendChild(catHeader);
+          
+          // delete cat
+          catHeader.querySelector('.cat-del-btn').addEventListener("click", async (e) => {
+             const cid = e.currentTarget.dataset.catId;
+             if(await showCustomConfirm(`繧ｫ繝・ざ繝ｪ繝ｼ縲・{cat.name}縲阪ｒ蜑企勁縺励∪縺吶°・滂ｼ井ｸｭ縺ｮ繝ｫ繝ｼ繝縺ｯ縲後き繝・ざ繝ｪ繝ｼ縺ｪ縺励阪↓遘ｻ蜍輔＠縺ｾ縺呻ｼ荏, "蜑企勁")) {
+                const newCats = categories.filter(c => c.id !== cid);
+                await updateDoc(doc(db, `artifacts/${appId}/servers/${currentServerId}`), { categories: newCats });
+                for(let r of roomsData.filter(r => r.categoryId === cid)) {
+                   await updateDoc(doc(db, `artifacts/${appId}/servers/${currentServerId}/rooms/${r.id}`), { categoryId: null });
+                   r.categoryId = null;
+                }
+                loadServerSettingsRooms();
+                if(typeof loadServerRooms === 'function') loadServerRooms(currentServerId);
+             }
+          });
+
+          const catRooms = roomsData.filter(r => r.categoryId === cat.id);
+          catRooms.sort((a,b) => a.order - b.order);
+          catRooms.forEach((r, i) => {
+            catDiv.appendChild(renderRoomItem(r, i, catRooms));
+          });
+          if (catRooms.length === 0) {
+            const emptySpan = document.createElement("div");
+            emptySpan.className = "text-xs text-gray-600 italic ml-4 mb-2";
+            emptySpan.textContent = "繝ｫ繝ｼ繝縺後≠繧翫∪縺帙ｓ";
+            catDiv.appendChild(emptySpan);
+          }
+          listEl.appendChild(catDiv);
         });
+
+        // Uncategorized rooms
+        const uncategorized = roomsData.filter(r => !r.categoryId);
+        uncategorized.sort((a,b) => a.order - b.order);
+        
+        if (uncategorized.length > 0) {
+           const uncatDiv = document.createElement("div");
+           uncatDiv.className = "mb-4";
+           uncatDiv.innerHTML = `<span class="text-xs font-bold text-gray-400 uppercase tracking-wider mb-2 ml-2 block">繧ｫ繝・ざ繝ｪ繝ｼ縺ｪ縺・/span>`;
+           uncategorized.forEach((r, i) => {
+              uncatDiv.appendChild(renderRoomItem(r, i, uncategorized));
+           });
+           listEl.appendChild(uncatDiv);
+        }
       }
 
       renderRoomsListUI();
@@ -11131,27 +11290,52 @@
 
     async function updateRoomOrderBoth(roomId, newOrder) {
       try {
-        
           await updateDoc(doc(db, `artifacts/${appId}/servers/${currentServerId}/rooms/${roomId}`), { order: newOrder });
-        
       } catch (e) { console.error(e); }
     }
+    
+    const createCategoryBtn = document.getElementById("createCategoryBtn");
+    if(createCategoryBtn) {
+      createCategoryBtn.addEventListener("click", async () => {
+        const name = prompt("繧ｫ繝・ざ繝ｪ繝ｼ蜷阪ｒ蜈･蜉帙＠縺ｦ縺上□縺輔＞");
+        if(!name) return;
+        loadingOverlay.classList.remove("hidden");
+        try {
+          const serverRef = doc(db, `artifacts/${appId}/servers/${currentServerId}`);
+          const snap = await getDoc(serverRef);
+          let cats = snap.data().categories || [];
+          if(!Array.isArray(cats)) cats = Object.values(cats);
+          cats.push({ id: "cat_" + Date.now(), name, order: cats.length * 10 });
+          await updateDoc(serverRef, { categories: cats });
+          loadServerSettingsRooms();
+          if (typeof loadServerRooms === 'function') loadServerRooms(currentServerId);
+          alertMessage("繧ｫ繝・ざ繝ｪ繝ｼ繧剃ｽ懈・縺励∪縺励◆", "success");
+        } catch(e) {
+          alertMessage("菴懈・縺ｫ螟ｱ謨励＠縺ｾ縺励◆", "error");
+        } finally {
+          loadingOverlay.classList.add("hidden");
+        }
+      });
+    }
 
     document.getElementById("createRoomInServerBtn").addEventListener("click", async () => {
       const name = document.getElementById("newRoomNameInput").value.trim();
+      const categoryId = document.getElementById("newRoomCategorySelect").value || null;
       if (!name) return;
       loadingOverlay.classList.remove("hidden");
       try {
         await addDoc(collection(db, `artifacts/${appId}/servers/${currentServerId}/rooms`), {
-          name, createdAt: serverTimestamp(), createdBy: userId
+          name, categoryId, createdAt: serverTimestamp(), createdBy: userId
         });
         document.getElementById("newRoomNameInput").value = "";
         await loadServerSettingsRooms();
+        if (typeof loadServerRooms === 'function') loadServerRooms(currentServerId);
         alertMessage("繝ｫ繝ｼ繝繧剃ｽ懈・縺励∪縺励◆", "success");
       } catch (e) { alertMessage("菴懈・縺ｫ螟ｱ謨励＠縺ｾ縺励◆", "error"); }
       finally { loadingOverlay.classList.add("hidden"); }
     });
 
+
     // 繝｡繝ｳ繝舌・邂｡逅・ち繝・     document.getElementById("ssMembersSection").parentElement; // (reference check)
     async function loadServerSettingsMembers() {
